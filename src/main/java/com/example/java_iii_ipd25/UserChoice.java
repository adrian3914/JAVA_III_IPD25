package com.example.java_iii_ipd25;

/* Create a set if labels (fields) that we can use throught the program to convey the user's choice */
public enum UserChoice {
    CREATE, GETBYID, REMOVEBYID, UPDATEBYID, NONE, EXIT
}
